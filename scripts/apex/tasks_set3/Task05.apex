// 5. Continue from above , Create a  similar logic as above, this time (startingNumber should be 
// greater than endingNumber in this case). 
// count down from starting number till the ending number.

Integer startingNumber = 60;
Integer endingNumber = 10;

for (Integer count = startingNumber ; count >=endingNumber ; count--) {
    System.debug(count); 

}